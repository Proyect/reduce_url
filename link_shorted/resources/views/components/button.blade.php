<button type="{{$type ?? "submit"}}" class="{{$class ?? "btn btn-primary btn-outline-light fw-bold"}} " id="{{$button ?? "button"}}" name="{{$name ?? "button"}}" {{$add ?? ""}}>
    <i class="{{$icon ?? "fas fa-file-contract"}}"></i>  {{$title ?? "Enviar Consulta"}}
</button>