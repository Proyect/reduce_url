<button type="{{$type ?? "submit"}}" class="{{$class ?? "btn btn-primary btn-outline-light fw-bold"}} ">
    <i class="{{$icon ?? "fas fa-file-contract"}}"></i>  {{$title ?? "Enviar Consulta"}}
</button>