<!-- Button trigger modal -->
<button type="{{$type ?? "button"}}" class="btn {{$class ?? "btn-primary"}}" data-bs-toggle="modal" 
    data-bs-target="#{{$id ?? "modal"}}">
    {{$title ?? ""}} 
  </button>